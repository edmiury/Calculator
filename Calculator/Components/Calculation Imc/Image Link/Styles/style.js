import {
    StyleSheet
} from 'react-native';

export const Style = StyleSheet.create({
    ViewImg: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'flex-start',
        backgroundColor: '#111'
    }, 

    img: {
        borderRadius: 50,
        width: 40,
        height: 40
    }
})