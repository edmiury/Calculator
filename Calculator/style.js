import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },

    txtInput: {
        marginBottom: 20,
        borderWidth: 0.5,
        borderRadius: 10,
        padding: 10,
        fontWeight: 'bold'
    }
})